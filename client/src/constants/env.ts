// Use in Api Requests
export const API = import.meta.env.VITE_APP_URL;

// Set Main Title For The App & addon it in every Page with the layout Logic
export const TITLE = import.meta.env.VITE_APP_TITLE;
